// Stub vitest pour `server-only` (voir vitest.config.ts). Marker Next-only :
// no-op côté tests, car l'environnement "node" de vitest est équivalent au
// runtime server Next et il n'y a pas de bundle client à protéger.
export {};
