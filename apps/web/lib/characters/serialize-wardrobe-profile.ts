/**
 * P1.4 — Sérialiseur dédié pour `Character.wardrobeProfile`.
 * Voir `lib/characters/serialize-body-state.ts` pour le contexte.
 */

export { serializeWardrobeProfileForPrompt } from "@/lib/retry/build-character-retry-hints";
