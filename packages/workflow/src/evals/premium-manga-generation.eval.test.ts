/**
 * Placeholder pour les évals golden / métriques génération premium manga.
 * Les scénarios lourds (LLM + images) seront branchés ici ; le fichier reste
 * skipped par défaut pour ne pas ralentir la CI.
 */
import { describe, it } from "vitest";

describe.skip("premium-manga-generation eval", () => {
  it("réservé aux runs eval manuels", () => {});
});
